/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://portfolio-kathryn-yangs-projects.vercel.app/",
  generateRobotsTxt: true,
};

console.log("Generating sitemap...");

module.exports = config;
